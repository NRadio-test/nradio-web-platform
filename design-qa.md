# 鲲鹏知识星图黑场点群设计 QA

final result: passed

2026-09-16，本地 `http://127.0.0.1:4173`，本机系统 Chrome/CDP；桌面 CSS 视口 `1440×900`、`deviceScaleFactor: 1`，手机模拟 CSS 视口 `390×844`、`deviceScaleFactor: 1`。这不是实体手机、iOS Safari 或线上发布验收。

## 源视觉、实现与同屏证据

- 风格参考：`/var/folders/s_/9gkxv4pn67565w1_v7jgvcjm0000gn/T/codex-clipboard-295212dd-3abe-4e98-be3a-bea61524136e.png`，原像素 `1438×1094`。目标只取黑底细密点群、暖光核和冷外围的视觉语言；参考截图右侧卡通、计数和其空闲长关系状射线不是本站资料语义目标。
- 实现截图：`/private/tmp/nradio-pointcloud-qa/galaxy-desktop-1440x900.png`（`1440×900`）、`galaxy-mobile-390x844.png`（`390×844`）、`home-desktop-1440x900.png`、`home-mobile-390x844.png`。截图是浏览器实际渲染，而非静态效果图。
- 全视图合并：`/private/tmp/nradio-pointcloud-qa/reference-vs-cloud-full.png`。源图裁掉顶部 `97px`，取 `1438×900` 后仅横向放大到 `1440×900`，与同 CSS 尺寸实现横向并置，避免把手机/桌面或不同密度混判。
- 点云聚焦合并：`/private/tmp/nradio-pointcloud-qa/reference-vs-cloud-focus-final.png`。从源图点云主体与实现星团中心各取约 `700×850` 并置，用于判断颗粒尺度、暖核/冷外缘、光学芒线和黑场对比；不是像素级复刻验收。
- 关键状态：`detail-desktop-1440x900.png`、`detail-mobile-390x844.png`、`transition-desktop-1440x900.png`、`transition-mobile-390x844.png`、`keyboard-preview-focused-1440x900.png`、`reduced-desktop-1440x900.png`。这些路径均位于同一 QA 目录。

## 迭代记录与结论

| 轮次 | 源/页面并排发现 | 修复 | 复核证据 |
|---|---|---|---|
| 首轮 | [P1] 点群过稀、过暗，首页小窗几乎看不出体积 | 数据附着微光加密、按深度提高细点可见度；桌面与手机同坐标投影 | 初轮 Chrome 截图、最终全视图/聚焦合并 |
| 第二轮 | [P2] 中心是硬竖亮线，移动端球偏小；转场快照星芯放大后偏粗 | 暖核心扩散为宽三维体积、移动投影增大、预览星芯收小 | 最终桌面/手机全屏、首页与转场截图 |
| 第三轮 | [P1] 参考图的暖白整片曝光仍明显强于孤立细点 | 仅以本帧已投影的暖核微粒在低分辨率光层累积曝光，再在锐利星点下合成；少量装饰星以短光学芒线补纹理 | 最终聚焦合并、Chrome 严格烟测及绘制采样 |

最终页面使用近黑背景、细白/暖白颗粒、宽而不规则的暖核心及冷外缘；没有贴 NASA 照片或底片选择器，没有大型霓虹 LED 光珠。参考图的极端白热中心和大量长线仍更夸张：这是明确接受的非像素复刻差异。本站只在选中知识块后画有同来源 URL 或至少两共享标签依据的最多九条关系线；空闲时不复制参考截图中可能被误读为真实知识关系的长射线。当前只有 `277` 颗实际知识亮星，不能借装饰把它说成数万块。剩余转场放大快照略粗属于瞬时 P3 光学差异；用户已表示当前关联与动效方向可用。

## 五项表面与响应式核对

- 字体/文案：仍用站点的中文/英文系统无衬线字与等宽 InfoID；详情正文 `16px/1.75` 清楚可滚动，图例明确“可选亮星”和非点击星雾/星芒的边界。旧知识卡片的字号不在本次风格重绘范围。
- 布局/间距：首页左标题与说明、下方搜索/标签/卡片顺序未动，右侧旧头像和两块统计改为入口点云；全屏顶部、左侧筛选、右侧桌面详情和手机底部详情没有实测控件碰撞。手机图例换行使面板略高，但不横向溢出、仍可滚动。
- 色彩：近黑幕布、低饱和暖/冷星点、站点青金焦点与深蓝浮控保持对比；不会用背景图吞掉知识点。聚焦截图中没有白图标或霓虹蓝灯式假银河。
- 图像质量：参考的点群是动态视觉，本站以原生 Canvas 2D 真实投影绘制而非贴图；暖曝光由当前投影的颗粒沉积生成，星核仍锐利。转场快照只短时保留球面连续性，全屏落地后用原生尺寸画布，命中不随 CSS 缩放偏离。
- 内容/交互状态：入口聚焦青框、Tab/Enter、悬停标题、选中标记/关联、筛选无匹配与匹配、详情关闭、来源链接和减少动态效果都在真实页面/脚本核对；来源资料和完整正文仍由 DOM 读取。

## 数据、功能与性能

`npm run check` 通过：`277` 条网站知识块、`277` 个唯一 InfoID 和有效同步坐标。最终浏览器前侧当前可命中的实点桌面 `86/86`、手机 `82/82` 均在画布视口内；装饰颗粒不在实点投影/命中列表。黑场含约 `14,064` 个三维非点击颗粒及 `1,850` 个离屏缓存远景点，但页面点数只显示真实 `277` 条，数据增长时颗粒数量自适应且设上限。

完整回归 `/private/tmp/nradio-pointcloud-qa/chrome-cdp-report.json` 覆盖桌面和手机：放大/拖拽后准确点选 InfoID 与逐字完整正文、装饰点不响应、InfoID/标题/正文/标签搜索、标签筛选和显示全部、键盘缩放/重置、`?id=<InfoID>`、Esc/按钮关闭、HTTP(S) 来源、返回、reduced-motion 无自转/跨页动画。末轮严格脚本 `smoke-report.json` 退出码 `0`，无摄影资源请求、JS exception 或 console error。入口经 Tab 五次出现 `:focus-visible` 外框，Enter 正常进入。

空闲 `3.17s` 内两视口均约 `96` 次 draw，即约 `30Hz` 的画布节流；桌面 draw 平均 `11.04ms`、p95 `12.0ms`、最大 `12.7ms`，手机视口平均 `7.00ms`、p95 `7.8ms`、最大 `8.4ms`。自动旋转仍按实际经过时间算角度，不靠帧数决定五分钟一圈。这些是本机 headless Chrome 样本，不是 Core Web Vitals、实体手机帧率或线上 CDN 结论。

本地 Python 服务的 `/api/knowledge` 返回 404，页面实测走本站 `/data/knowledge.json` 回退；生产 API 响应与生产 CSP 头未在这次本地静态服务器中强制验证。代码未在本次任务中推送或发布。
