# 知识星图摄影重绘 QA

final result: passed

2026-09-16，本地 `http://127.0.0.1:4173`，本机系统 Chrome/CDP。桌面 `1440×900` 与手机视口 `390×844` 都验收了真实页面；手机视口模拟不等于实体手机或 iOS Safari。截图与机器可复核报告位于 `/private/tmp/nradio-photographic-qa/`，其中 `reference-vs-page.png` 把用户第 1 张参考照片与全屏页面同尺寸并排。照片是设计参考，不是页面的未授权资源。

## 视觉与状态

- 影像：默认全屏和首页预览同用本站自托管的 NASA M101 照片。暖白星核、灰蓝旋臂与暗尘带在真实截图中可见，不再由蓝青径向渐变和大光晕模拟。可选 NGC 1333 英仙座尘云与 NASA 银河中心照片均在预载后稳态显示，署名及官方影像链接随选择更新。用户第 2 张参考照片带第三方摄影师署名，未复用。
- 亮度与知识点：首轮桌面截图发现明亮旋臂、尤其密集银河中心原图会吞没白色知识点。现以温和深蓝黑线性遮罩、银河中心专属降亮及真实点不足 1px 的暗色光学边界解决；最终 `galaxy-desktop-1440x900.png`、`plate-milkyway-desktop-1440x900.png` 和相应手机截图仍保留真实照片纹理。真实点是细小光学星核，不是旧版均匀发光灯珠。照片的星、装饰微光均不可点击。
- 排版与布局：首页左侧标题说明、下方原搜索/标签/卡片结构保持；右侧旧头像与两块统计消失，改为 M101 观测窗。桌面星图顶部、左侧搜索/筛选/底片、左下署名、右下视角与桌面右侧详情无交叠；手机顶部面板、底部详情、署名/提示/视角控件在对应状态均可用。正文独立滚动，InfoID 及来源链接可读。暖白文字/深色功能面板和金色焦点形成清晰层级；旧知识卡片的原站点排版未被重做。
- 动效：首页到星图的 `transition-v4-desktop-1440x900.png` 和手机中间帧显示 M101 摄影球由小变大，无白闪。首轮中间帧的预览 Canvas 放大曾产生模糊的大白点，已将转场快照减至 `.28` opacity，让官方照片主导过渡；全屏真实 Canvas 保持原尺寸以保证命中。`prefers-reduced-motion` 模拟时两尺寸均直接导航，无离场/入场动画 DOM。
- 无模板化替代物：没有 ImageGen 假照片、CSS 伪银河、头像占位、通用 sparkle 徽章或远程视觉脚本。文字图例解释影像/装饰与知识块的区别、关联线依据。键盘聚焦和按钮目标在截图及 DOM 中保留；200% 缩放、屏幕阅读器、实体触屏和强制色模式尚未做设备级验收。

## 真实数据与功能

- `npm run check`：277 条网站知识块、277 个唯一 InfoID/有效坐标，检查通过。坐标由同步流程按标签、来源和 InfoID 稳定生成，首页/全屏共用同一 `GalaxyScene`。点数由实际加载值显示，不把 AstrBot 后续可能切出的内部向量分块当成当前粒度。
- Chrome 两尺寸均实测：点选、滚轮缩放/拖拽转动后再点选，详情 InfoID、标题和完整正文逐字对应被点知识块；最终桌面缩放拖动后 `(545.92, 585.80)`、手机视口 `(95.42, 392.21)` 均命中 `upload-20260803-678c211dc3-04`《访客 Wi‑Fi 隔离要同时做三层和二层》。独立装饰粒子点击不会选中知识块。
- InfoID 搜索得到 1 个结果，`C2000MAX` 标签高亮 `21/277`，显示全部恢复；结果选择转到前侧并打开全文。Esc/关闭按钮、`?id=<InfoID>`、来源 HTTP(S) 链接、返回知识库、三底片切换都通过。桌面与手机 JS exception、console error 均为 0；导航时取消旧请求出现的 `ERR_ABORTED` 不代表当前底片失败，每张本地 JPEG 均有 HTTP 200。
- 本机 headless Chrome 空闲 `3.17s` 中约 96 次 Canvas draw（目标约 30Hz），桌面平均 `3.20ms`/p95 `4.0ms`/最大 `4.7ms`，手机视口平均 `2.06ms`/p95 `3.0ms`/最大 `5.6ms`。交互/定位绘制目标改为 16ms 间隔，但实体设备流畅度、LCP/INP 与线上 CDN 尚未测量。最终 CSS 转场减淡只做了针对性桌面/手机中间帧复核，未无故重跑已通过的完整功能套件。

## 边界

代码与资源仅在本地仓库实现，未提交、推送或线上发布。`_headers` 的 CSP 要求本站脚本/影像/样式，当前文件路径符合；Python 本地服务不施加线上 CSP 响应头，因此这里不声称生产环境 CSP 已在浏览器强制验证。图片的详细署名和官方页面链接在星图的底片署名控件中；NASA/ESA 不为本站背书。
